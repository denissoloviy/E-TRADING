@model bool
<ul class="nav navbar-nav">
    @if (Model)
    {
        <li>@Html.ActionLink("Catalogs", "Index", "Catalogs")</li>
    }
    @if (!Model)
    {
        <li>@Html.ActionLink("Users managment", "UserList", "Users")</li>
    }
</ul>
@Html.Partial("_LoginPartial")